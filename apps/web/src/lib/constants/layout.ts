/** Minimum primary-pane share when split view is on (percent). */
export const PRIMARY_PANE_SPLIT_MIN_PERCENT = 30;

/** Maximum primary-pane share when split view is on (percent). */
export const PRIMARY_PANE_SPLIT_MAX_PERCENT = 80;

/** Default primary-pane share (percent). */
export const PRIMARY_PANE_SPLIT_DEFAULT_PERCENT = 62;
