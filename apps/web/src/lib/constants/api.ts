/** Default HTTP request timeout for API calls (milliseconds). */
export const DEFAULT_API_TIMEOUT_MS = 30_000 as const;

/** Default WebSocket connection attempt timeout (milliseconds). */
export const DEFAULT_WS_CONNECT_TIMEOUT_MS = 15_000 as const;
